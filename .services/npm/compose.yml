volumes:
  service_npm_volume:
  service_npm_letsencrypt:

services:
  service-npm:
    image: jc21/nginx-proxy-manager:latest
    ports:
      - 80:80
      - 81:81
      - 443:443
    volumes:
      - service_npm_volume:/data
      - service_npm_letsencrypt:/etc/letsencrypt
