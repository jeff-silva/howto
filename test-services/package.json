{
  "name": "test-services",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose down && BUN_CMD=dev docker compose up -d --build --remove-orphans",
    "start": "docker compose down && BUN_CMD=start docker compose up -d --build --remove-orphans",
    "stop": "docker compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
