services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    volumes: [./backend/src:/app/src]

  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    volumes: 
      - ./frontend/src:/app
      - /app/node_modules
